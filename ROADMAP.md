# VideoPlay 功能补充路线图（对照 PotPlayer）

> 基于 2026-08-09 代码审计整理。本文档用于指导后续迭代，聚焦“与 PotPlayer 相比明显缺失、且能显著提升日常观影体验”的功能，并按可落地性分阶段推进。

---

## 1. 文档范围与约定

- **目标读者**：项目维护者与贡献者。
- **功能来源**：以 PotPlayer 主流版本为参照，同时兼顾 SDL3 + FFmpeg 技术栈的可行性。
- **优先级定义**：
  - **P0（Must）**：缺失会导致基础播放体验明显不如竞品。
  - **P1（Should）**：常用功能，完成后可大幅提升可用性。
  - **P2（Could）**：增强型功能，适合作为长期扩展。
  - **P3（Won't for now）**：实现成本高或与本项目定位偏差较大，暂不推荐。

---

## 2. 当前已具备的能力

| 类别 | 功能 |
|------|------|
| 播放核心 | 音视频播放、变速 (0.25x–4.0x)、AB 循环、章节、播放列表、剧集自动识别 |
| 解码 | FFmpeg 软件解码 + 硬件解码 (D3D11VA/DXVA2/VAAPI/CUDA/VideoToolbox) |
| 字幕 | SRT/ASS/VTT/PGS、字幕样式、字幕同步偏移 |
| 音频 | 音量/静音、多音轨、音频滤镜（均衡器、语音/低音/夜间、动态归一化、限幅器） |
| UI/交互 | 全屏、无边框窗口、始终置顶、截图、进度条、菜单栏、控制栏 |
| 网络 | 基本 http/https 流媒体播放、断线重连 |
| AI | 视频分析/摘要/内容搜索 |

---

## 3. 功能差距总览

### 3.1 画面与视频处理

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| 亮度/对比度/饱和度/色调/伽马 | P0 | 高 | 小 | 最常用的画面调节，PotPlayer 默认就有 |
| 去隔行 (Deinterlace) | P1 | 中 | 中 | 对老视频/电视录制内容影响大 |
| 视频旋转/镜像/裁剪/缩放 | P1 | 中 | 中 | 画面几何变换，手机视频/监控常用 |
| HDR Tone Mapping | P1 | 中 | 大 | SDR 显示器播放 HDR 内容刚需 |
| 降噪/锐化 | P2 | 中 | 中 | 后处理滤镜 |
| 色彩空间/色域切换 | P2 | 低 | 中 | BT.601/BT.709/BT.2020 等 |
| 外挂 Pixel Shader / 滤镜链 | P3 | 高 | 很大 | 高级扩展，架构改造大 |

### 3.2 音频

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| 音频同步延迟/提前 | P0 | 高 | 小 | 目前只有字幕同步，音画不同步无法调 |
| 音频输出设备选择 | P1 | 高 | 小 | 用户常有耳机/扬声器/HDMI 切换需求 |
| 音频通道映射 | P1 | 中 | 中 | 立体声转单声道、左右声道交换等 |
| 音高/节奏调整 | P2 | 中 | 大 | 保持音高的变速变调 |
| 音频 Bitstreaming | P3 | 低 | 大 | S/PDIF / HDMI 透传 |

### 3.3 字幕生态

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| 自动加载同名字幕 | P0 | 高 | 小 | 自动匹配 `.srt/.ass/.vtt` |
| 在线字幕搜索/下载 | P1 | 高 | 中 | 射手网 / OpenSubtitles 等 API |
| 同时显示两条字幕 | P2 | 中 | 中 | 例如双语字幕 |

### 3.4 播放交互

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| 进度条缩略图预览 | P1 | 高 | 中 | 拖动进度时显示当前帧缩略图 |
| 用户自定义书签 | P1 | 高 | 中 | 手动打点，独立于章节 |
| 睡眠定时/播放完毕关机 | P2 | 中 | 小 | 夜间观影常用 |
| 缩略图导航视图 | P3 | 中 | 大 | 类似 PotPlayer 章节/缩略图浏览 |

### 3.5 网络与扩展

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| M3U / IPTV 播放列表 | P1 | 高 | 中 | 流媒体直播刚需 |
| 平台 URL 解析 (YouTube/B 站等) | P1 | 高 | 中 | 可调用 yt-dlp / you-get |
| DLNA / 投屏 | P3 | 中 | 很大 | 协议栈复杂，偏离核心 |

### 3.6 录制与捕获

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| GIF 片段录制 | P1 | 高 | 中 | 社交分享常用 |
| 视频录制 | P2 | 中 | 大 | 完整录制当前播放 |
| 帧提取序列 | P2 | 低 | 中 | 批量导出关键帧 |
| 音频可视化 / 频谱 | P3 | 中 | 大 | 视觉装饰，优先级较低 |

### 3.7 信息与统计

| 功能 | 优先级 | 收益 | 工作量 | 说明 |
|------|--------|------|--------|------|
| 实时统计面板 (fps/码率/丢帧/CPU/GPU) | P1 | 高 | 中 | 调试与性能感知 |
| 详细媒体信息窗口 | P2 | 中 | 小 | 完整流信息展示 |
| 快捷键自定义 / 鼠标手势 / 皮肤 | P3 | 中 | 很大 | 可配置性，后期考虑 |

---

## 4. 分阶段实施路线图

### Phase 1：基础体验补全（2 周）

**目标**：补齐最常用的播放体验缺口，改动小、见效快、风险低。

| 功能 | 涉及模块 | 关键改动 | 验收标准 |
|------|----------|----------|----------|
| 音频同步调节 | `FFmpegPlayer` / `AudioPlayer` / `app_events.cpp` / `sdlrenderer_events.cpp` | 在音频重采样或输出阶段增加毫秒级延迟/提前偏移；菜单/快捷键增加 `G/H` 或 `Ctrl+Shift+←/→` 调节 | 快捷键可调，OSD 显示当前偏移，重启保持 |
| 自动加载同名字幕 | `app.cpp` / `subtitleparser.cpp` | 打开文件后扫描同目录同名 `.srt/.ass/.vtt`，按 `.chs/.cht/.en` 等后缀选择优先级 | 存在同名字幕时自动加载，字幕菜单正确显示 |
| 画面基础参数 (eq 滤镜) | `FFmpegPlayer` / `settingsdialog.cpp` / `sdlrenderer_events.cpp` | 接入 FFmpeg `eq` 滤镜：`brightness/contrast/saturation/hue/gamma`；UI 提供滑块或快捷键 | 五个参数实时可调，关闭播放器可记忆 |
| 用户书签 | `core/settings.cpp` / `app_playback.cpp` / `sdlrenderer_ui.cpp` | 新增 `Bookmark` 结构，持久化按文件路径索引；快捷键 `Ctrl+B` 添加/删除 | 可添加、跳转、删除、持久化 |

**依赖与风险**
- `eq` 滤镜接入需确认 `hwaccel` 下是否可用；若硬件解码不支持软滤镜，需回退到软件解码或在 SDL 渲染层做颜色矩阵。
- 书签持久化会增大 `Settings` 的 JSON 体积，建议按文件哈希或路径做键。

---

### Phase 2：播放增强（3–4 周）

**目标**：提升对多样化内容（老视频、HDR、手机拍摄）的兼容性。

| 功能 | 涉及模块 | 关键改动 | 验收标准 |
|------|----------|----------|----------|
| 去隔行 (Deinterlace) | `FFmpegPlayer` | 接入 `bwdif` / `yadif` 滤镜，提供 关闭/自动/Bob/线性 模式 | 隔行视频播放时画面不抖动 |
| 视频旋转/镜像/裁剪 | `FFmpegPlayer` 或 `SDLRenderer` | `transpose` / `crop` 滤镜或 SDL 渲染层变换；菜单提供 90/180/270/水平翻转/垂直翻转 | 画面方向正确，性能无明显下降 |
| 音频输出设备选择 | `core/audioplayer.cpp` / `settingsdialog.cpp` | SDL3 枚举 `SDL_GetAudioPlaybackDevices`，切换设备时重建 `SDL_AudioDevice` | 设置中列出设备，切换后音频正常输出 |
| 进度条缩略图预览 | `SDLRenderer` / `FFmpegPlayer` | 鼠标悬停/拖动进度条时，单独解码附近一帧并缩放为 120×68 缩略图 tooltip | 悬停 100ms 内显示缩略图，不阻塞主循环 |

**依赖与风险**
- 缩略图预览需要避免与主解码线程冲突，可考虑在 `FFmpegPlayer` 中加一个独立的 seek + decode-one-frame 接口。
- 去隔行会显著增加 CPU/GPU 负载，需在硬件解码路径上验证。

---

### Phase 3：网络与字幕生态（4–6 周）

**目标**：从本地播放器扩展到“能看直播、能下字幕”的通用播放器。

| 功能 | 涉及模块 | 关键改动 | 验收标准 |
|------|----------|----------|----------|
| M3U / IPTV 播放列表 | `app_playlist.cpp` / `episodedetector.cpp` | 解析 `.m3u/.m3u8`，识别 `#EXTINF` 频道名，按直播流处理 | 可打开 M3U 文件并切台，直播流断线重连 |
| 在线字幕搜索/下载 | 新增 `subtitles/onlinesub` 模块 | 对接 OpenSubtitles / 射手网；下载后自动加载 | 字幕菜单中搜索、选择、下载、加载 |
| 平台 URL 解析 (yt-dlp) | `core/ffmpegplayer.cpp` / 新增 `network` 模块 | 调用 `yt-dlp --get-url` 等子进程获取真实流 URL；注意 GPL 兼容 | 粘贴 YouTube/B 站链接可播放 |
| 同时显示两条字幕 | `SDLRenderer` / `subtitleparser.cpp` | 支持主字幕 + 副字幕两个轨道同时渲染（上下位置分离） | 可开启“双语字幕”模式 |

**依赖与风险**
- 在线字幕需处理 API key、网络超时、用户隐私（文件名上传）。
- `yt-dlp` 为 GPL 工具，作为外部子进程调用不影响本项目的 GPL 合规性，但需明确依赖说明。

---

### Phase 4：高级与生态（6 周以上，长期）

**目标**：面向高级用户与发烧友的功能。

| 功能 | 涉及模块 | 关键改动 | 验收标准 |
|------|----------|----------|----------|
| HDR Tone Mapping | `FFmpegPlayer` / `SDLRenderer` | 检测 HDR 元数据，使用 `tonemap` / `zscale` 滤镜或 SDL 渲染时做 tone mapping | HDR 视频在 SDR 显示器上明暗正常 |
| GIF 片段录制 | `FFmpegPlayer` / `SDLRenderer` | 用户选区 A-B，截取该段解码后编码为 GIF | 输出 GIF 动图可正常播放 |
| 实时统计面板 | `SDLRenderer` / `FFmpegPlayer` | 显示 fps、码率、丢帧、CPU/GPU 占用 | 按 `Tab` 或菜单切换显示/隐藏 |
| 快捷键/鼠标手势自定义 | `settings.cpp` / `sdlrenderer_events.cpp` | 将键位绑定配置化，支持用户自定义 | 用户可在设置中修改快捷键 |
| 皮肤主题 | `SDLRenderer` / `settings.cpp` | 将颜色/尺寸常量抽出为可配置文件 | 可切换浅色/深色/自定义主题 |

---

## 5. 推荐启动顺序

对于下一次迭代，建议按以下顺序启动：

1. **音频同步调节**（P0，改动最小，路径清晰）
2. **自动加载同名字幕**（P0，纯文件扫描，无 FFmpeg 风险）
3. **画面基础参数**（P0，收益最大，UI 可复用现有设置对话框）
4. **用户书签**（P1，提升对长视频/剧集的导航体验）

这四项合计约 2 周工作量，能显著提升日常播放体验，且不会触及架构大改。

---

## 6. 非功能性补充建议

| 方向 | 建议 |
|------|------|
| 性能 | 评估 `SDL_Renderer` 后端；在高分辨率下可考虑 D3D11/Metal 零拷贝渲染 |
| 测试 | 为新增的音视频滤镜、书签、字幕扫描补充 Google Test 单元测试 |
| 文档 | 新功能实现后同步更新 `AGENTS.md` 中的“Critical Gotchas”与快捷键说明 |
| 跨平台 | Linux 路径与快捷键需单独验证；硬件解码列表已区分平台，注意滤镜后端差异 |

---

## 7. 参考文件

- `AGENTS.md` — 项目架构与关键约束
- `docs/design_episode_playlist.md` — 播放列表与剧集交互设计
- `src/core/ffmpegplayer.cpp` — 解码/滤镜/播放控制核心
- `src/renderer/sdlrenderer.cpp` / `sdlrenderer_ui.cpp` — 渲染与 UI
- `src/core/settings.h` / `settings.cpp` — 配置持久化
